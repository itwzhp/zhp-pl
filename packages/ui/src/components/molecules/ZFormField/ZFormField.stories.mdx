import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, text, object } from '@storybook/addon-knobs';
import { withA11y } from '@storybook/addon-a11y';
import ZFormField from './ZFormField.vue';

<Meta title="Molecules/Form Field" component={ZFormField} />

# FormField

<Preview>
  <Story name="Common">
    {{
        components: { ZFormField },
        props: {
          label: {
            default: text('label', 'Rodzaj wydarzenia')
          }
        },
        data(){
          return {
            value: ''
          }
        },
        template: `<ZFormField
          v-model="value"
          :label="label"
        />`,
    }}
  </Story>
</Preview>

# Props

<Props of={ZFormField} />
